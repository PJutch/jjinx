server {
    default

    route = / {
        file index.html
    }

    route ^~ /index.html {
        file index.html
    }

    route ~ .* {
        status 404
    }

    route ^~ /get-host {
        body "Host: $host\n"
    }
}
